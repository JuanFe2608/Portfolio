Drop your images here:

- headshot.jpg     — Hero portrait (recommend 600×800px, JPG/WebP, optimized)
- nyc-vessel.jpg   — About-section photo (recommend 600×800px portrait)

If you change the file names, also update the `src` props in:
- components/Hero.tsx   (line with src="/images/headshot.jpg")
- components/About.tsx  (line with src="/images/nyc-vessel.jpg")
